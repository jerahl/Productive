# @beacon/web

Placeholder for the Beacon web client. Phase 1 scaffolds the real app here
(React 19 + TypeScript + Vite + Tailwind v4 + TanStack Query) and builds the
persistent shell + Tasks view pixel-matched to `docs/design-reference/`.

Nothing to build yet — this directory only reserves the workspace slot.
